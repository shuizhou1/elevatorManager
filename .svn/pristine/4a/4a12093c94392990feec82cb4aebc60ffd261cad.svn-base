/**
 * 生产环境
 */
;(function () {
  window.SITE_CONFIG = {};

  // api接口请求地址
    window.SITE_CONFIG['baseUrl'] = '//' + window.location.host + '/ebiliftiot-admin';
    window.SITE_CONFIG['gzelev'] = '//' + window.location.host + '/gzelev';

  // cdn地址 = 域名 + 版本号
  window.SITE_CONFIG['domain']  = './'; // 域名
  window.SITE_CONFIG['version'] = '';   // 版本号(年月日时分)
  window.SITE_CONFIG['cdnUrl']  = window.SITE_CONFIG.domain + window.SITE_CONFIG.version;
  console.log('domain', window.SITE_CONFIG.domain)
  console.log('vserdion', window.SITE_CONFIG.version)
})();
